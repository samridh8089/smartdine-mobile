/**
 * Battery Helper — Disabled popups as per UI preference.
 * Only handles background permission checks silently.
 */

export async function checkAndPromptBatteryOptimization() {
  return;
}

export async function openBatteryOptimizationSettings() {
  return;
}
